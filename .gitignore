*.iml
.gradle/
.kotlin/
# local.properties holds the SDK path AND the private dacha coordinates.
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/app/build/
*.apk
*.keystore
*.jks

# NOT ignored (must be committed): gradle/wrapper/gradle-wrapper.jar,
# gradle/libs.versions.toml, gradle.properties (non-secret flags only).
